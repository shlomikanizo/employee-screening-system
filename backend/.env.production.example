# Production Environment Variables
# Copy this file and configure for your production environment

# Application Settings
APP_NAME="Employee Screening Form"
APP_VERSION="1.0.0"
DEBUG=False
HOST=0.0.0.0
PORT=${PORT}  # Render will provide this

# Database
# For production, consider PostgreSQL:
# DATABASE_URL=postgresql://user:password@host:5432/dbname
DATABASE_URL=sqlite:///./job_worker.db

# Notification Service - Choose ONE option:

# Option 1: Email (RECOMMENDED for Production)
USE_EMAIL=true
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM_NAME=Employee Screening System

# Option 2: WhatsApp Business API (Official)
# WHATSAPP_API_URL=https://graph.facebook.com/v18.0
# WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
# WHATSAPP_ACCESS_TOKEN=your_access_token
# WHATSAPP_VERIFY_TOKEN=your_verify_token

# Note: WhatsApp Web (USE_WHATSAPP_WEB) is NOT supported in production
# It only works on localhost

# Google Maps
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Company Info
COMPANY_NAME="Your Company Name"
COMPANY_ADDRESS="Your Full Address"
COMPANY_LAT=32.008679
COMPANY_LNG=34.749669
COMPANY_LOGO_URL=https://your-domain.com/logo.png

# Job Details (Default values - can be changed per form)
JOB_TITLE="Job Position"
JOB_DESCRIPTION="Job description goes here"
JOB_SALARY_RANGE="10,000-15,000 ₪"
JOB_LOCATION="Tel Aviv, Israel"

# Frontend URL (IMPORTANT: Set this to your Netlify URL)
FRONTEND_URL=https://your-app-name.netlify.app

# Security
# Generate a strong secret key for production!
# You can use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-strong-secret-key-here
ALGORITHM=HS256

# File Storage
UPLOAD_DIR=./uploads
PDF_OUTPUT_DIR=./pdfs
